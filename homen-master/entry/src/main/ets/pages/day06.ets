@Entry
@Component
struct Day06 {
  @State message: string = 'Hello World';
  scroller:Scroller = new Scroller()
  build() {
    Scroll(this.scroller){
      Column({space:20}) {
        Row() {
          Column(){
            Image($r('app.media.ic_wechat'))
              .width(40)
            Text('微信')
          }
          Column(){
            Image($r('app.media.ic_tongxunlu'))
              .width(40)
            Text('通讯录')
          }
          Column(){
            Image($r('app.media.ic_zhinanzhen'))
              .width(40)
            Text('发现')
          }
          Column(){
            Image($r('app.media.ic_my'))
              .width(40)
              .fillColor(Color.Green)
            Text('我')
          }
        }
        .justifyContent(FlexAlign.SpaceAround)
        .width('100%')
        .padding({top:20,bottom:20})
        .border({
          style:{top:BorderStyle.Solid},
          width:{top:10},
          color:Color.Gray
        })
        .backgroundColor('#F0F0F0')
        Column({space:20}){
          Column({space:5}){
            Image($r('app.media.ic_aixin'))
              .width(50)
            Text(12.9+'万')
              .fontColor(Color.White)
              .fontSize(18)
              .fontWeight(FontWeight.Bold)
          }
          Column({space:5}){
            Image($r('app.media.ic_duihua'))
              .width(50)
            Text(1.2+'万')
              .fontColor(Color.White)
              .fontSize(18)
              .fontWeight(FontWeight.Bold)
          }
          Column({space:5}){
            Image($r('app.media.ic_shoucang'))
              .width(50)
            Text(2.3+'万')
              .fontColor(Color.White)
              .fontSize(18)
              .fontWeight(FontWeight.Bold)
          }
          Column({space:5}){
            Image($r('app.media.ic_butuijian'))
              .width(50)
            Text('少推荐')
              .fontColor(Color.White)
              .fontSize(18)
              .fontWeight(FontWeight.Bold)
          }
          Column({space:5}){
            Image($r('app.media.ic_fenxiang'))
              .width(50)
            Text(8.5+'万')
              .fontColor(Color.White)
              .fontSize(18)
              .fontWeight(FontWeight.Bold)
          }
        }
        .padding(10)
        .backgroundColor(Color.Black)
        .width(100)
        Row(){
          Row(){
            Text(){
              Span('1465')
                .fontSize(20)
                .fontWeight(FontWeight.Bolder)
              Span('获赞')
                .fontColor(Color.Gray)
            }
            Text(){
              Span('13')
                .fontSize(20)
                .fontWeight(FontWeight.Bolder)
              Span('朋友')
                .fontColor(Color.Gray)
            }
            Text(){
              Span('20')
                .fontSize(20)
                .fontWeight(FontWeight.Bolder)
              Span('关注')
                .fontColor(Color.Gray)
            }
            Text(){
              Span('110')
                .fontSize(20)
                .fontWeight(FontWeight.Bolder)
              Span('粉丝')
                .fontColor(Color.Gray)
            }
          }
          .width('100%')
          .justifyContent(FlexAlign.SpaceAround)
        }
        .width('100%')
        Row(){
          Row(){
            Image($r('app.media.ic_wechat1'))
              .width(100)
            Column({space:10}){
              Text('微信支付')
                .textAlign(TextAlign.Start)
                .width(150)
                .fontWeight(FontWeight.Bold)
              Text('已支付￥13.00')
                .width(150)
                .textAlign(TextAlign.Start)
                .fontColor(Color.Gray)
                .fontSize(14)
            }

          }
          Text('08:52')
            .fontColor(Color.Gray)
            .fontSize(14)
        }
        .padding({right:20})
        .justifyContent(FlexAlign.SpaceBetween)
        .width('100%')
      }
      .height('100%')
    }
  }
}